const defaultInfo = {
    data() {
        return {
            testText: "Hello world"
        }
    },
    methods: {
        testAlert() {
            alert("alert test for ok")
        }
    }
}

export default defaultInfo;